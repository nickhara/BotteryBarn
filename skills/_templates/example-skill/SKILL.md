---
name: example-skill
description: |
  Use this skill to <one-sentence statement of what the skill accomplishes>. Trigger
  this skill when <conditions>. Outputs <what the user/agent gets>.
# version: 0.1.0
# inputs:
#   - name: <input-name>
#     description: <what the agent should gather before starting>
# outputs:
#   - name: <output-name>
#     description: <artifact produced>
---

# example-skill

## Purpose

<One short paragraph: what this skill does and the value it delivers.>

## Preconditions

- <Condition 1, e.g., "user has provided a repo path">
- <Condition 2>

## Procedure

1. **<Step name>** — <imperative description of what to do, with concrete commands or tool calls>.
2. **<Step name>** — <…>. If `./resources/<file>` is needed, load it.
3. **<Step name>** — <…>.

## Output

Describe the exact artifact the agent should hand back (file written, markdown block printed, JSON returned, …).

## References

- `./references/<doc>.md` — <when to read>
